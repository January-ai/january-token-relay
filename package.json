{
  "name": "january-token-relay",
  "version": "0.1.0",
  "private": true,
  "description": "One-file token relay for the January Developer API: exchanges your API key for short-lived client tokens, minted only for users your own login system has verified.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "dev": "vercel dev"
  },
  "dependencies": {
    "jose": "^6.0.11"
  }
}
