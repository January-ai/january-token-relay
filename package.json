{
  "name": "january-token-relay",
  "version": "0.2.0",
  "private": true,
  "description": "One-file token relay for the January Developer API: your app trades a relay token for short-lived client tokens, and your API key never ships on a phone.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "dev": "vercel dev"
  }
}
